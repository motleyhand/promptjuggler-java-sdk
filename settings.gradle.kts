pluginManagement {
    repositories {
        // The maven-publish plugin (>= 0.14) ships to Maven Central, not the Gradle portal.
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "promptjuggler-java"
